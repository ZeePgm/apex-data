name = "apex-data-api"
main = "src/index.ts"
compatibility_date = "2025-06-25"
account_id = "298672578e36aa170df1d30f29703a0d"

# Cloudflare KV 用于缓存 API 响应
# kv_namespaces = [
#   { binding = "API_CACHE", id = "xxx" }
# ]

# API keys are configured as Cloudflare Worker secrets, not committed here.
# Set production secrets with:
#   npx wrangler secret put TRACKER_API_KEY
#   npx wrangler secret put MOZAMBIQUE_API_KEY
#
# For local development, copy .dev.vars.example to .dev.vars and fill real values.

[observability]
enabled = true
